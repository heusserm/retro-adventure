<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the app's surface color so the window does not flash white
         before Compose draws its first frame. -->
    <style name="Theme.RetroAdventure" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
</resources>
